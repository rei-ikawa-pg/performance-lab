# Performance Lab — 実装タスク一覧

## Phase 1: 基盤構築（Week 1 / 8h）

### 1-1. プロジェクト初期化（2h）

- [x] Next.js (App Router) + TypeScript プロジェクト作成
- [x] Tailwind CSS + shadcn/ui セットアップ
- [x] ESLint + Prettier 設定
- [x] Vitest + React Testing Library セットアップ
- [x] Playwright セットアップ
- [x] `.env.local.example` 作成（必要な環境変数一覧）
- [x] プロジェクトディレクトリ構成作成（`src/app`, `src/components`, `src/lib`, `src/types`）

### 1-2. Google認証（2h）

- [x] NextAuth.js v5 インストール・設定（`src/lib/auth.ts`）
- [x] Google OAuth Provider 設定
- [x] `api/auth/[...nextauth]/route.ts` 作成
- [x] NextAuth middleware で保護ルート設定（`/dashboard`, `/report/*`）
- [x] ヘッダーにログイン/ログアウトボタン実装
- [x] セッション状態に応じたUI切り替え

### 1-3. PageSpeed Insights API PoC（2h）

- [ ] Google Cloud Console で API キー取得
- [x] `POST /api/measure` Route Handler 作成
- [x] APIレスポンスの型定義（`src/types/pagespeed.ts`）— 必要フィールドのみ抽出
- [x] APIクライアント関数（`src/lib/pagespeed.ts`）
- [x] レスポンスの正規化関数（生APIレスポンス → アプリ内型への変換）

### 1-4. D3.js PoC（2h）

- [x] D3.js インストール・型定義確認
- [x] `GaugeChart.tsx` のプロトタイプ実装（SVGベースゲージ）
- [x] React useRef + useEffect での D3.js 統合パターン確認
- [x] 動作しない場合の Recharts フォールバック判断

---

## Phase 2: MVP計測機能（Week 2 / 8h）

### 2-1. URL入力 → 計測フロー（3h）

- [x] `MeasureForm.tsx` — URL入力 + バリデーション + 計測ボタン
- [x] TanStack Query で `/api/measure` を呼び出す mutation hook 作成
- [x] ローディング状態UI（Skeleton / Spinner）
- [x] エラーハンドリングUI（API失敗、無効URL、Rate Limit超過）
- [x] 計測完了後 `/report/[id]` への遷移

### 2-2. 計測結果表示（3h）

- [x] `ScoreCard.tsx` — メトリクスカード（スコア値 + Good/Needs Improvement/Poor カラー表示）
- [x] スコア判定ユーティリティ（`src/lib/utils.ts`）— 閾値に基づくステータス判定
- [x] `GaugeChart.tsx` — D3.js ゲージチャート完成版（アニメーション付き）
- [x] `/report/[id]/page.tsx` — 計測結果ページ全体のレイアウト
- [x] レスポンシブ対応（モバイル / タブレット / デスクトップ）

### 2-3. 改善提案レポート（2h）

- [x] `AuditList.tsx` — audits配列を重要度順にソート・フィルタして表示
- [x] 各auditのスコア・カテゴリ・説明・改善方法を表示
- [x] 展開/折りたたみUIで詳細を表示

### 2-4. Rate Limiting（1h 内 Phase 2 バッファ含む）

- [x] `src/lib/rate-limit.ts` — in-memory Rate Limiter 実装（Map + タイムスタンプ）
- [x] `/api/measure` に Rate Limiting ミドルウェア適用
- [x] Rate Limit 超過時の 429 レスポンス + UI表示

---

## Phase 3: 品質向上 + 計測履歴（Week 3 / 8h）

### 3-1. Supabase連携・計測履歴保存（4h）

- [ ] Supabase プロジェクト作成
- [x] テーブルマイグレーション作成（`measurements`, `urls`）
- [x] RLS（Row Level Security）ポリシー設定 — ユーザーは自分のデータのみアクセス可
- [x] Supabase クライアント設定（`src/lib/supabase.ts`）
- [x] 計測実行時に結果を自動保存する処理追加
- [x] `GET /api/measurements` — 履歴一覧API
- [x] ダッシュボード画面（`/dashboard/page.tsx`）— 登録URL + 最新スコア一覧

### 3-2. テスト（2.5h）

- [x] ユーティリティ関数のユニットテスト（スコア判定、レスポンス正規化）
- [ ] `/api/measure` Route Handler のユニットテスト（モック使用）
- [x] Rate Limiter のユニットテスト
- [x] Playwright E2E: ログイン → URL入力 → 計測結果表示フロー

### 3-3. レスポンシブ・アクセシビリティ（1.5h）

- [x] 全画面のレスポンシブ検証・調整
- [x] カラーコントラスト検証
- [x] キーボード操作検証
- [x] aria-label / role 属性追加（特にチャート）

---

## Phase 4: 仕上げ・デプロイ（Week 4 / 8h）

### 4-1. CI/CD（1.5h）

- [ ] GitHub Actions ワークフロー作成（lint, type-check, test）
- [ ] Vercel プロジェクト接続・自動デプロイ設定
- [ ] 環境変数の Vercel 設定

### 4-2. パフォーマンス最適化（2h）

- [ ] D3.js の dynamic import 化
- [ ] Server Components / Client Components の適切な分離
- [ ] 画像最適化（next/image, OGP画像）
- [ ] 自アプリの Lighthouse スコア計測・90+ 達成

### 4-3. SEO・OGP（1h）

- [ ] Metadata API でメタタグ設定
- [ ] OGP画像作成・設定
- [ ] robots.txt, sitemap 設定

### 4-4. ドキュメント・仕上げ（1.5h）

- [ ] README.md（スクリーンショット付き、技術スタック、セットアップ手順）
- [ ] `.env.local.example` の最終更新
- [ ] デモ用のスクリーンショット/GIF撮影

### 4-5. 最終検証（2h）

- [ ] 本番環境での全機能動作確認
- [ ] エッジケーステスト（無効URL、タイムアウト、認証切れ）
- [ ] クロスブラウザ検証（Chrome, Firefox, Safari）
- [ ] モバイル実機検証

---

## スコープ判断ポイント

| タイミング    | 判断内容                                                 |
| ------------- | -------------------------------------------------------- |
| Week 1 終了時 | D3.js PoCが難航 → Recharts にフォールバック              |
| Week 2 終了時 | Must機能未完 → Week 3 の Supabase をカットし Must に集中 |
| Week 3 終了時 | テスト不足 → Week 4 でテスト優先、SEO/OGP を簡略化       |
